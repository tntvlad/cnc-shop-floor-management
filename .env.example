# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=cnc_shop_floor
DB_USER=postgres
DB_PASSWORD=changeme

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=8h

# File Upload Configuration
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=10485760

# Client Folders Configuration
# Path on the host machine where client folders are stored
CLIENTS_HOST_PATH=/mnt/nas_NAS_Storage_LOGICAL/shares/Prelucrare/1-Clienti
# Path inside the Docker container (usually don't need to change this)
CLIENTS_ROOT_PATH=/data/clients

# CORS Configuration (use * to allow access from any IP)
FRONTEND_URL=*
